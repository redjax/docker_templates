# Mise Tools

Docker image that pre-installs tools with [mise](https://mise.jdx.dev). Find tool versions on [mise-versions](https://mise-versions.jdx.dev) and add them to the [`.mise.toml`](./.mise.toml) file to install them during the Docker build.
