## Docker Compose overlay to change the behavior of the gickup container.
#  Run the schedule on-demand and exit

services:
  gickup:
    restart: "no"
    volumes:
      ## Note you must create this file!
      - ./config/on-demand.yml:/gickup/conf.yml
