# Minecraft - Cobbleverse

Modded Minecraft server, running the [Cobbleverse modpack](https://modrinth.com/modpack/cobbleverse).

## Setup

- Copy [`.env`](./.env.example) to `.env`
- Edit values, i.e. set a different port or modpack version
- Run `docker compose up -d`

